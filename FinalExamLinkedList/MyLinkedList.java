public class MyLinkedList {
    // add(Node newNode), add(Node newNode, int index)
    // remove(), remove(int index)
    // search(String data)
    // empty()
    // getSize()
    // printList()
    // LinkedList(), LinkedList(Node head)
}